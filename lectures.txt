lecture 1 2026-09-02
prof douglas densmore

remember: email prof about the midterm and final exam - how theyre being held and extra time etc
 - he will let me know regarding midterm n final exam when he finds out himself
 - have let him know about the name thing

 BEFORE NEXT WEEK:
 print out and sign syllabus consent document
 download/complete combinational logic and sequential logic quizzes
 overview of combinational logic in one lectureread chapter 1
 read chapter 2

quizzes on combinational logic and sequential logic - do these before next week to know if i should drop the class
"everyone will get an a, a-, b unless you really screw around"

densmore thesis on formal verification

office in cilse building (biology building) to go see arcade game

dougd@bu.edu
617-358-6238
thursdays 10/11AM in office 403 in cilse building

david r smith, paul franzon
verilog styles for synthesis of digital systems

midterm 10% - long, written, hard and u wont finish it
3 programs, 30% (10, 10, 10)
5 homework assignments 2% each
final project 40%
final exam 10%

3 programs:
 - logic synthesis
 - FPGA designer
 - physical design

 EDA electronic design automation

 HDL hardware description language (ie verilog etc)

 RTL register transfer-level (design)

combinationial: output is result on function on inputs only.
sequential: with latches in order. function of inputs and history of inputs

have to recognise which parts are sequential and combinational

specification --(verilog or vhdl description)-> code --(simulation)-> functional simulation
code --(synthesis)->netlist--(simulate)->post synthesis simulation

netlist is actual maps of gates and wires etc
post synthesis simulation should be same as functional simulation. v important


design flow overview - look up starter info on lecture 1 pdf notes
 - design specification: describe what you want, performance reqs, time reqs, etc
 - design partition: decide what is hardware and what is software. currently pretty automated
 - design entry: start designing it
 - simulation and functional verification
 - design integration and verification
 - pre synthesis sign off
 - synthesize and map the gate-level netlist
 - post synthesis validation (check the timing! critical paths!)
 - post synthesis verification
 - test generation and fault simulation
 - NOT COVERED: cell placement, scan chain, clock tree insertion, cell routing
 - NOT COVERED: DRC
 - NOT COVERED: extract parasitics
 - design sign-off


=======================================================================================
lecture 2 2026-09-08
accidentally missed.

combinational logic
notes here

binary numbers
binary arithmetic
gates/cmos layout
two level logic
sum of products (SOP) and product of sums (POS)
K-maps (karnaugh maps)
Q-macluskie, AKA tabular method
Cube/mintern/prime implicant/implicant/essential prime implicant.
do NOT say that combinational circuit is one that doesnt have cycles - can have cycles!
only when the output is dependent on only the inputs is it combinational logic.

=======================================================================================
lecture 3 2026-09-10
sequential logic
next week fpga tutorial

1nanosecond ns = 1gigahertz GHz

timing diagrams/waveforms


SR latches, D latches, D flipflop
- SR latches
s= set, r=reset.
cross coupled NOR gates. pic in slides.
can't trigger set and reset at the same time.
storing 1 bit. press call button, light stays on; press again, light turns off.
therefore must take into account both input and current state - needs memory!
XOR(ButtonPressed?, Current state)
bit storage using an SR latch. 
remember that in practice that delays exist when values are getting updated! worry of oscillation - causing a glitch.
designer might try to avoid issue using external circuit.

 - level-sensitive SR latch
SR latch but with a clock added. only allows data to update when the clock is high.

 - D latches
level sensitive D latch - inserted inverter ensures R always opposite of S (reset automatically when 'set' is off)
stays value of D input until clock updates, then checks if new D value.
transparency - accepting changes to D for entire length of clock high. if too long a clock, d latch may erroneously update

 - D flipflop
d flipflop is similar to D latch except it updates on the clock edge not the clock signal
(posedge/negedge)
made of two D latches, linked up. diagram on slides.
solves issue for not knowing how many latches a signal travels through when clock=1

scan chain - chain of flipflops
also used in shifters (shifting binary left/right)

setup time - time needed to change input value
hold time - time taken til clock
clock-to-Q - how long til output is updated

if period is 20ns, frequency = 1/20 nanoseconds = 50 megahertz megahertz

register is a collection of flipflops in order.




FSM creation
finite state machine - a machine that has a finite number of states.
contains a register that stores the (value of) the current state.
contains combinational logic that is used to determine the next state.
the current state is what is coming out of the register.
state gets updated at clock edge.


 - Mealy machines
 output is function of the current state combined with the input.
 can have asynchronous outputs depending on input changes.
 output on edge from state to state: mealy machine

 - Moore machines - "Moore is Less/Less is Moore"
 output is function of the current state
 only clock-synchronous outputs.
 outputs only associated with states then moore machine.

State Transition Diagram (STD)
initial state: arrow going in from outside diagram.
edges: arrows pointing from state to state.
edges (arrows) labelled with the inputs required.

to make FSM truth table:
Input 1| Input 2| [...] | Current state|-->| Output 1 (of current state) | [ ... ] | Next state 
^all these values are in binary! rows can be concatenated into one big binary number technically

One Hot: 100 states - 100 state lines in table.
all states 'added together' or probability value. should equal 1.
OR them all together, using binary arithmetic techniques
A + ~AB + ~A~B = 1


Minimum bit encoding: minimum bits needed to represent each state.

FSM State transition verification
- exhaustive and complete transitions

FSM state encoding



implication chart
can't be told apart by outputs?
then, can't be told apart by inputs?
save states.
IMG IN PHONE

equivalent if no matter what state currently in, can't tell difference by output.

state reduction/State minimization:
reduce number of states without changing behaviour
partitioning method
1. partition/group states by their outputs.
2. if FOR EACH STATE input is same and goes to same group, then good! otherwise, if next state is in different group, not same.
3. break up not same states in same group - retry. note that if one state in group only, then can't be broken down further.

partitioning for large FSM too complex for humans - now heuristics for it to be done automatically

one hot encoding: [???]

output encoding: encoding method where output is same as state.

REMEMBER TO UPDATE SLIDES IN REPO - SLIDES EXPANDED.
PRINT OUT N DO THE QUIZZES ANALOGUE STYLE NOT JUST DIGITAL STYLE (EVEN THOUGH DONE DIGITAL STYLE)
next: quiz review, combinational and sequential logic wrap up.
sept 17: verilog start

=======================================================================================
lecture 4 2026-09-15
last day of review

quick check for 2s complement binary -
add to positive number (ie -745 +745 and should carry to 0)

2s complement unique respresentation of 0 unlike 1 complement binary form

variables
literals
conjunction of literals = Cube
all represented: minterms
implicants
prime implicants
essential prime implicants
Max terms (0s)
 - an implicate
boolean algebraic postulates


=======================================================================================
lecture 5 2026-09-17
(lecture 4 slides)

may not be possible to convert mealy to moore and back.
possible but an odd question to be asked/ 

WARNING:
most programming languages describe what you want to happen.
verilog describes what the chip is and must be.
don't write the code from top to bottom - think about the code happening all at once in a block.

structural description: describing connections of components.
write so that the synthesiser has no choice but to make it the way you want

functional description: describing what the code does.
written so the code gets the same answer as what you wanted but not necessarily how you expected.

simulation description: doesn't always synthesise. system verilog.

'module' describes component in a circuit. notes on slides.

"accidental synthesis of a latch from an unspecified condition" <BAD

INPUTS ARE MONITORED. UPON CHANGE, OUTPUT IS CHANGED.
can be done continuously or over [...]

module name_of_module (port list 1, port list 2)
    input  in1 in2;
    [SEE SLIDE AND ONLINE WRITING GUIDES]

don't write some things structural some things behavioural. write structural stuff and put into behavioural as a module.

verilog 26 built in combinational primitives (and, or, nand, etc)


outputs first, inputs after. (his preference)
case sensitive.
lines terminate w semicolon;

verilog operators on slides

reduction: does operation to all of the bits in the singular input wire.

verilog dot notation for when one doesn't like the order of ports

Verilog Styles for Synthesis of Digital Systems - look through it at densmore's next office hour

=======================================================================================
lecture 6 2026-09-22
(lecture 5 slides) 

analog simulation
 - divide time into slices
 - update info in whole circuit at each slices
 - used by SPICE (environment for circuit simulation)
 - - allows detailed modeling of current n voltage
 - - computationally intensive very slow.
 

bad simulator: looks at every gate and update on input change
better simulator: update each gate only when an input for the gate changes.
best simulator: see better simulator, stop when gate output doesnt change
^^^event driven simulator.
(like verilog) (event: change in net's value, two components. value, time)

data structure "TIME WHEEL"
data structure in simulation that determines the order of the events processed
events placed on time wheel in time order and taken out of the head to process
stores CHANGES IN VALUES IE EVENTS
start from the first change available:
 - tie of times, choose either one
 - put changed values due to previous change events at END OF WHEEL

X unknown value/ambiguous
Z high impedance no value

in reality, though:
no actual 'x'. 'x' is bad.
see slides lecture 5 to see the x/z gate logic

net family includes wired-and (wand) and wired-or (wor) - maybe unsynthesizable
also includes supply0 and supply1, to model ground and power connections respectively

testbench! UUT = DUT.
special verilog module with no ports, timing, events etc

= blocking assignment
<= nonblocking assignment, looked at equivalently

FOUND GROUP FRANCIS AND. DUMA?? have francis on discord now

https://www.youtube.com/playlist?list=PLkw8tPrWxGDZdCLviboa4n4_xF2qAikhX
be evil suggest vigenere cipher encoder decoder idk

inertial delay - gates
 - if fixed by delay, then inertial delay. gate more stable
transport delay - nets(wires)
 - if spike in value is shifted, then it's due to the wire. ie transport delay

event descheduling caused by inertial delay.

unit delay (delay all by 1) better than 0 delay.
depth of circuit affects speed! easier also to see propagation through circuit.

=======================================================================================
lecture 7 2026-09-24
(lecture 6 slides) 

can link fpgas together

benefits of PFGAs:
implementation of random logic - easier changes at system level to modify one device
can eliminate need for full custom chips

prototyping: ensemble of gate arrays used to emulate a circuit to be manufactured.
better/more/faster debugging

configuration: bits that determine logic function+interconnect
CLB: combinational logic block
LUT: look up table
IOB: I/O block, I/O pin + associated logic and electronics


LUT BASED: input signals used as pointer to look up table (and output)

look up cascade logic block

CORDIC: coordinate rotation digital computer. non linear arithmetic in digital logic.

for example, calculating e^x:
if only 16 points between 0 and 6, then can hard-code values.

by assigning values to a wire, synthesis tool can use boolean logic to simplify the LUT entries and make it more area efficient.
if always small, static, hardcoded values - useful to take this approach. otherwise, use register.
can also interpolate between fixed points using adders and subtractors
how to know if good enough? use area and power used as metrics

even/odd bitonic sorters - similar to merge sort.
sorting N elements, need NlogN cycles.

even-odd sorter - similar to bubble sort.
sorting N elements, need N cycles.

alternative sorter(bad) count sort implemented in hardware

other sorters:
sort 1 element every cycle, turning off unused portions of circuit during cycle


multiplication in digital logic can only be implemeneted in terms of additions or bit shifts for power-of-2 multiplication.
booth algorithm for multiplication:
can break down multiplication into set of partial product computations.
508 = 512-4
508 = 0001 1111 1100
512 = 0010 0000 0000
004 = 0000 0000 0100

a*b = (a1-a2)*b and both a1 and a2 are powers of 2.

